let character: string;
let num: number;
let isLogged: boolean;

let ninjas: string[] = [];
ninjas.push("hi");

let mixedArr: (string | number | boolean | object | string[])[] = [];
mixedArr.push({});
mixedArr.push(["hi", "hello"]);

let uid: string | number;

let ninja: object;
ninja = [];

let ninja2: { name: string; age: number };

console.log("hi");
