"use strict";
const arr = ["hi", 24, false]; // normal arr can put any type at any index
arr[1] = "some string"; // xan do this
let tuple; // have to create this first for the tuple to work as a tuple.
tuple = ["s", 2, true];
// tuple[0] = 5 cant do this now
